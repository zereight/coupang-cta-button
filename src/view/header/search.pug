.search-container
  input.fake-search-input(placeholder='Ctrl + K (⌘ + K)')
  .search-popup.hide-search
    s_sidebar
      s_sidebar_element
        <!-- 검색 -->
        .inner-layer
          s_search
            input(
              id="search-input"
              type='text',
              placeholder='Enter',
              name='[##_search_name_##]',
              value='[##_search_text_##]',
              onkeypress='if (event.keyCode == 13) { [##_search_onclick_submit_##] }'
            )
            button(onclick='[##_search_onclick_submit_##]')
